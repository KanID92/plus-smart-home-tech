spring:
  application:
    name: gateway
  config:
    import: configserver:
  cloud:
    config:
      fail-fast: true
      retry:
        userRandomPolicy: true
      discovery:
        enabled: true
        serviceID: config-server

eureka:


